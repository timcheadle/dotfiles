#!/usr/bin/env ruby

require 'CSV'

csv = CSV($stdin, headers: false, converters: :all)

headers = csv.shift
data = csv.shift

header_len = headers.max_by(&:length).length

headers.each_with_index do |column, i|
  printf("%5d %-#{header_len}s (%s)\n", i + 1, headers[i], data[i])
end
